package practices;

public class Trials {

	public static void main(String[] args) {
		/*char [] first = new char [] {'a', 'b', 'c', 'd'};
        char [] last = new char [] {'w', 'y', 'z'}; 
        char [] aux = new char [7];

        System.arraycopy(first, 0, aux, 0, first.length); 
        first[0] = 'k' ; 
        System.arraycopy(last, 0, aux, 4, last.length); 
        System.out.println(aux[3]); 

        first = last; 
        last[0] = 'm' ; 
        System.out.println(first[0]); 
        

        System.out.println();
        
        int [] array1={1,3,5}, array2={1,3,5};

        boolean b = array1==array2;
        System.out.println(b);*/
	}
}

